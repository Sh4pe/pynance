coverage:
  precision: 2
  round: down
  range: "90...100"

  status:
    project: no
    patch:
      default:
        # coverage may fall by <1.0% and still be considered "passing"
        threshold: 1.0%
    changes: no

comment:
  layout: "header, diff, changes, tree"
  behavior: default